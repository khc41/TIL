## 📅 2026-02-22
### 📚 읽은 개발 서적: [데이터 중심 애플리케이션 설계](https://www.yes24.com/Product/Goods/59566585?pid=123487&cosemkid=go16406746660905354&utm_source=google_pc&utm_medium=cpc&utm_campaign=book_pc&utm_content=ys_240530_google_pc_cc_book_pc_12110%EB%8F%84%EC%84%9C2&utm_term=%EB%8D%B0%EC%9D%B4%ED%84%B0%EC%A4%91%EC%8B%AC%EC%95%A0%ED%94%8C%EB%A6%AC%EC%BC%80%EC%9D%B4%EC%85%98%EC%84%A4%EA%B3%84&gad_source=1&gad_campaignid=6762605740&gbraid=0AAAAAD79Irrfvaqb4HDi15JUqflihSilR&gclid=Cj0KCQjw3aLHBhDTARIsAIRij5-GpSKsMKO8XIATcjMBStXbk-93KGuGTmn0kK3NAbsbDtX417fNAIUaAih4EALw_wcB)
#### 4장. 부호화
- 애플리케이션은 필연적으로 시간이 지남에 따라 변하므로 변경 사항을 쉽게 적용할 수 있게 시스템을 구축해야 한다.
- 애플리케이션이 변하면 저장하는 데이터도 변하는데 대규모 애플리케이션에서 코드 변경은 대개 즉시 반영할 수 없으므로 양방향 호환성을 유지해야 한다.
##### 데이터 부호화 형식
- 데이터는 객체, 구조체, 배열 등으로 CPU에서 효율적으로 접근하고 조작할 수 있는 메모리에 유지되는 것과 파일에 쓰거나 네트워크를 통해 전송하는 바이트열 형태로 부호화한 것이 있다.
  - 부호화(직렬화, 마샬링): 인메모리 표현 -> 바이트열
  - 복호화(파싱, 역직렬화, 언마샬링): 바이트열 -> 인메모리 표현
- **언어별 형식**
  - 프로그래밍 언어에 내장된 부호화 라이브러리는 매우 편리하지만 심각한 문제점이 많다.
    - 특정 프로그래밍 언어와 묶여 있어 다른 언어에서 읽기 어렵고, 다른 시스템과 통합하는 데 방해가 된다.
    - 동일한 객체 유형의 데이터를 복원하려면 복호화 과정이 임의의 클래스를 인스턴스화할 수 있어야하는데 종종 보안 문제의 원인이 된다.
    - 데이터 버전 관리를 나중에 생각하게 돼서 상위, 하위 호환성의 불편한 문제가 있다.
    - 자바의 내장 직렬화는 성능이 좋지 않고 비대해지는 부호화로 유명하다.
- **JSON과 XML, 이진 변형**
  - JSON은 XML 대비 단순하다.
  - JSON, XML, CSV의 문제
    - XML과 CSV는 수와 숫자로 된 문자열을 구분할 수 없다. JSON은 문자열과 수를 구분하지만 정수와 부동소수점 수를 구별하지 않고 정밀도를 지정하지 않는다.
    - 큰 수를 다룰 때 부동소수점 수를 다루는 언어에서 파싱할 때 부정확해질 수 있다. 트위터에선 JSON 수로 한번, 10진 문자열로 한번해서 정확하게 숫자를 파싱하지 않는 문제를 해결한다.
    - JSON과 XML은 유니코드 문자열을 지원하지만 이진 문자열은 지원하지 않는다. 이진 데이터를 Base64를 사용해 텍스트로 부호화해 이런 제한을 피한다.
    - XML, JSON 모두 스키마를 지원한다.
    - CSV는 스키마가 없어서 로우와 칼럼의 의미를 정의하는 작업을 애플리케이션이 해야한다.
  - **이진 부호화**
    - 작은 데이터셋의 경우에는 부호화 형식 선택으로 얻는 이득이 무시할 정도지만 테라바이트 정도가 되면 데이터 타입이 큰 영향을 미친다.
    - JSON과 XML은 이진 형식과 비교하면 훨씬 더 많은 공간을 사용한다.
    - 메시지팩은 JSON용 이진 부호화 형식인데 JSON보다 약간 작은 공간 절약이 되지만 가독성을 해친다.
- **스리프트와 프로토콜 버퍼**
  - 아파치 스리프트와 프로토콜 버퍼는 같은 원리를 기반으로 한 이진 부호화 라이브러리다.
  - 스키마 정의를 사용해 코드를 생성하는 도구가 있고 스키마의 레코드를 부호화, 복호화할 수 있다.
  - 스리프트는 바이너리프로토콜, 컴팩트 프로토콜이라는 다른 이진 부호화 형식이 있다.
    - 바이너리프로토콜
      - 필드 이름이 없고 숫자로된 필드 태그를 포함한다.
    - 컴팩트프로토콜
      - 동일한 정보를 더 작은 공간으로 부호화한다.
      - 필드 타입과 태그 숫자를 단일 바이트로 줄이고 가변 길이 정수를 사용해서 부호화한다.
  - 프로토콜 버퍼는 비트를 줄여 저장하는 처리 방식이 약간 다르지만 컴팩트프로토콜과 매우 비슷하다.
  - **필드 태그와 스키마 발전**
    - 스키마가 필연적으로 시간에 따라 변하는 것을 스키마 발전이라 한다.
    - 각 필드는 태그 숫자로 식별하고 데이터타입을 주석으로 단다.
    - 필드에 새로운 태그 번호를 부여하는 방식으로 스키마에 새로운 필드를 추가할 수 있고, 예전 코드에서 새로운 코드로 기록한 데이터를 읽으려는 경우 해당 필드를 무시함으로써 상위 호환성을 유지한다.
    - 스키마의 초기 배포 후에 추가되는 모든 필드는 optional이거나 기본값을 가지게 함으로써 하위호환성을 유지한다.
  - **데이터타입과 스키마 발전**
    - 필드의 데이터타입을 변경하는 것은 값이 정확하지 않거나 잘릴 위험이 있다.
    - 프로토콜 버퍼는 필드에 repeated 표시자가 있는데, 동일한 필드 태그가 여러번 나타날 수 있고 optional 필드를 repeated로 변경해도 문제가 없다.
- **아브로**
  - 사람이 편집할 수 있는 아브로 IDL, 기계가 더 쉽게 읽을 수 있는 JSON 기반 언어 두 가지로 스키마를 사용한다.
  - 스키마에 태그 번호가 없어서 부호화하면 길이가 가장 짧다.
  - 이진 데이터를 파싱하려면 스키마에 나타난 순서대로 필드를 살펴보고 스키마를 이용해 각 필드의 데이터타입을 미리 파악해야 한다.
    - 읽기, 쓰기 간 스키마가 불일치하면 데이터 복호화가 정확하지 않다.
  - **쓰기 스키마와 읽기 스키마**
    - 데이터를 알고있는 스키마 버전을 사용해 아브로로 부호화할때 해당 스키마를 쓰기 스키마라고 한다.
    - 데이터를 특정 스키마로 복호화할 때 이 스키마를 읽기 스키마라고 한다.
    - 쓰기 스키마와 읽기 스키마가 동일하지 않아도 되며 호환 가능하면 된다.
      - 필드 순서가 달라도 스키마 해석에서 이름으로 필드를 일치시킨다.
  - **스키마 발전 규칙**
    - 호환성을 유지하기 위해서는 기본값이 있는 필드만 추가하거나 삭제할 수 있다.
      - 기본값이 없는 필드를 추가하면 하위 호환성이 깨지고 기본값이 없는 필드를 삭제하면 상위 호환성이 깨진다.
    - null을 허용하려면 유니온 타입을 사용해야 한다.
      - optional과 required 표시자를 가지고 있지 않다.
    - 별칭으로 필드 이름 변경이 가능해서 하위 호환성이 있지만 상위 호환성은 없다.
  - **그러면 쓰기 스키마는 무엇인가?**
    - 모든 레코드에 전체 쓰기 스키마를 포함시킬 수 없다.
      - 많은 레코드가 있는 대용량 파일
        - 모두 동일한 스키마로 부호화된 수백만 개의 레코드를 포함한 큰 파일의 경우 파일 시작 부분에 한번만 쓰기 스키마를 포함시킨다.
      - 개별적으로 기록된 레코드를 가진 데이터베이스
        - 모든 부호화된 레코드의 시작 부분에 버전 번호를 포함하고 데이터베이스에서는 스키마 버전 목록을 유지한다.
      - 네트워크 연결을 통해 레코드 보내기
        - 네트워크 연결을 통해 연결 설정에서 스키마 버전 합의를 하고 연결을 유지하는 동안 합의된 스키마를 사용한다.
  - **동적 생성 스키마**
    - 스키마에 태그 번호가 포함되지 않아 동적 생성 스키마에 적합하다.
    - 관계형 스키마와 아브로 스키마를 매핑했을 때 데이터베이스 스키마가 변경되면 새로운 아브로 스키마를 생성하고 새로운 아브로 스키마로 데이터를 내보내면 된다.
  - **코드 생성과 동적 타입 언어**
    - 스리프트와 프로토콜 버퍼는 코드 생성에 의존한다.
      - 복호화된 데이터를 위해 효율적인 인메모리 구조를 사용하고 데이터 구조에 접근하는 프로그램을 작성할 때 IDE에서 타입 확인과 자동 완성이 가능한 정적 타입 언어에 유용하다.
    - 동적 타입 언어는 코드 생성하는 것이 중요하지 않다.
    - 아브로는 정적 타입 언어를 위해 코드 생성을 선택적으로 제공한다.
  - **스키마의 장점**
    - 스키마 언어는 XML, JSON 스키마보다 훨씬 간단하며 자세한 유효성 검사 규칙을 지원한다.
    - 많은 데이터 시스템이 이진 부호화를 독자적으로 구현하기도 한다.
      - 질의를 데이터베이스로 보내고 응답을 받을 수 있는 네트워크 프로토콜이 있다.
    - 이진 부호화의 장점
      - 부호화된 데이터에서 필드 이름을 생략할 수 있기 때문에 이진 JSON 변형보다 크기가 훨씬 작을 수 있다.
      - 복호화를 할 때 스키마가 필요하기 때문에 스키마가 최신 상태인지 확신할 수있다.
      - 스키마 데이터베이스를 유지하면 변경이 적용되기 전에 상위 호환성과 하위 호환성을 확인할 수 있다.
      - 정적 타입 언어는 컴파일 시점에 스키마 타입체크를 할 수 있다.
##### 데이터플로 모드
- 프로세스 간 데이터를 전달하는 방법
- **데이터베이스를 통한 데이터플로**
  - 기록하는 프로세스는 부호화, 읽는 프로세스는 복호화한다.
  - 저장하는 일은 미래의 자신에게 메시지를 보내는 일처럼 생각할 수 있고 하위 호환성이 필요하다.
  - 다양한 프로세스가 데이터베이스에 접근하게 되면 새로운 버전의 코드로 기록된 다음 현재 수행 중인 예전 버전의 코드로 값을 읽을 가능성이 있어 상위 호환성도 필요하다.
  - 애플리케이션에서 데이터베이스 값을 모델 객체로 복호화하고 나중에 재부호화한다면 변환 과정에서 알지 못하는 필드가 유실될 수 있다.
  - **다양한 시점에 기록된 다양한 값**
    - 애플리케이션은 새로운 버전으로 완전히 대체할 수 있지만 데이터베이스는 그렇지 않아 이를 데이터가 코드보다 더 오래 산다라고 한다.
    - 데이터를 새로운 스키마로 마이그레이션하는 방법도 있지만 대용량 데이터셋 대상으로는 값비싼 작업이기 때문에 대부분 피한다.
    - 기존 데이터를 다시 기록하지 않고 널을 기본값으로 갖는 새로운 칼럼을 추가하는 간단한 스키마 변경을 허용한다.
  - **보관 저장소**
    - 데이터베이스의 스냅숏을 수시로 만든다고 가정하면 데이터 덤프는 보통 최신 스키마를 사용해 부호화한다.
- **서비스를 통한 데이터플로: REST와 RPC**
  - 네트워크를 통해 통신할 때 가장 일반적인 방법은 클라이언트와 서버 두 역할로 배치하고 API로 통신하는 것이다.
  - 서버 자체가 다른 서비스의 클라이언트일 수 있는데 이런 방식을 서비스 지향 설계, 마이크로서비스 설계라고 한다.
  - 마이크로서비스 아키텍처의 핵심 설계 목표는 서비스를 배포와 변경에 독립적으로 만들어 애플리케이션 변경과 유지보수를 더 쉽게 할 수 있게 만드는 것이다.
  - 서버와 클라이언트가 사용하는 데이터 부호화는 서비스 API의 버전 간 호환이 가능해야 한다.
  - **웹 서비스**
    - 서비스와 통신하기 위한 기본 프로토콜로 HTTP를 사용한다.
    - REST와 SOAP 두가지 방법이 있다.
    - REST
      - HTTP 원칙을 토대로한 설계 철학
      - URL을 사용해 리소스를 식별하고 캐시 제어, 인증, 콘텐츠 유형 협상에 HTTP 기능을 활용한다.
      - REST 원칙에 따라 설계된 API를 RESTful API라고 한다.
      - 스웨거 등을 사용해 제품 문서를 기술한다.
    - SOAP
      - 네트워크 API 요청을 위한 XML 기반 프로토콜
      - HTTP와 독립적이며 대부분의 HTTP 기능을 사용하지 않는다.
      - 다양한 기능을 추가한 광범위하고 복잡한 여러 관련 표준(웹 서비스 프레임워크)를 제공한다.
      - 웹 서비스 기술 언어(WSDL) 이라고 부르는 XML 기반 언어를 사용한다.
        - 정적 타입 언어에서는 유용하지만 동적 타입 언어에서는 유용성이 떨어진다.
        - 복잡하고 사람이 읽기 힘들어서 도구 지원과 코드 생성과 IDE에 크게 의존한다.
  - **원격 프로시저 호출(RPC) 문제**
    - RPC 모델은 원격 네트워크 서비스 요청을 같은 프로세스 안에서 특정 프로그래밍 언어의 함수나 메서드를 호출하는 것과 동일하게 사용 가능하게 해준다.(위치 투명성)
    - 로컬 함수 호출은 예측 가능하지만 네트워크 요청은 예측이 어렵다.
      - 네트워크 요청은 타임아웃으로 결과 없이 반환될 수 있다.
      - 멱등성을 적용하지 않으면 재시도는 작업이 여러 번 수행되는 원인이 된다.
      - 함수 호출보다 훨씬 느리고 지연 시간이 매우 다양하다.
      - 모든 매개변수는 바이트열로 부호화해야해서 큰 객체면 문제가 생긴다.
    - **RPC의 현재 방향**
      - gPRC는 프로토콜 버퍼를 이용한 RPC 구현이다.
        -하나의 요청과 하나의 응답뿐만 아니라 시간에 따른 일련의 요청과 응답으로 구성된 스트림을 지원한다.
      - 일부는 서비스 찾기(service discovery)를 지원한다.
      - RPC 프로토콜은 성능이 우수하지만 RESTful API는 실험과 디버깅에 적합하고 지원과 생태계가 다양하다.
    - **데이터 부호화와 RPC의 발전**
      - 모든 서버를 갱신하고 나서 모든 클라이언트를 갱신하는 것을 가정하면 요청은 하위 호환성만 필요하고 응답은 상위 호환성만 필요하다.
      - RPC가 조직 경계를 넘나드는 통신에 사용되는 것은 서비스 호환성 유지를 더욱 어렵게 한다.
      - 호환성을 깨는 변경이 필요하면 서비스 제공자는 보통 여러 버전의 서비스 API를 함께 유지한다.
      - RESTful API는 URL이나 HTTP Accept 헤더에 버전 번호를 사용하는 방식이 일반적이다.
- **메시지 전달 데이터플로**
  - RPC와 데이터베이스 간 비동기 메시지 전달 시스템
  - 클라이언트 요청을 낮은 지연 시간으로 다른 프로세스에 전달한다는 점에서 RPC와 유사하지만, 메시지를 직접 네트워크 연결로 전송하지 않고 메시지 브로커나 메시지 지향 미들웨어라는 중간 단계를 거쳐 전송한다.
  - 장점
    - 메시지 브로커가 버퍼처럼 동작할 수 있기 때문에 시스템 안정성이 향상된다.
    - 죽었던 프로세스에 다시 메시지를 전달 가능해 유실을 방지할 수 있다.
    - 송신자가 수신자의 IP 주소나 포트 번호를 알 필요가 없다.
    - 하나의 메시지를 여러 수신자로 전송할 수 있다.
    - 송신자와 수신자와 분리된다.
  - **메시지 브로커**
    - 프로세스 하나가 메시지를 이름이 지정된 큐나 토픽으로 전송하고 브로커는 해당 큐나 토픽 하나 이상의 소비사 또는 구독자에게 메시지를 전달한다.
    - 토픽은 단방향 데이터플로만 제공한다.
    - 메시지 브로커는 특정 데이터 모델을 강요하지 않고 메시지는 일부 메타데이터를 가진 바이트열이므로 모든 부호화 형식을 사용할 수 있다.
  - **분산 액터 프레임워크**
    - 액터 모델은 단일 프로세스 안에서 동시성을 위한 프로그래밍 모델이다.
      - 스레드(경쟁 조건, 잠금, 교착 상태와 연관된 문제들)를 직접 처리하는 대신 로직이 액터에 캡슐화된다.
    - 액터는 로컬 상태를 가질 수 있고 비동기 메시지의 송수신으로 다른 액터와 통신하고 메시지 전달을 보장하지 않는다.
    - 분산 액터 프레임워크에서 이 모델은 여러 노드 간의 애플리케이션 확장에 사용된다.
##### 정리
- 많은 서비스가 순회식 업그레이드가 필요하고 이는 발전성에 도움이 많이 된다.
- 약간의 주의를 기울이면 상하위 호환성과 순회식 업그레이드가 가능하다.

#### 💡 느낀 점
- 인강에서 값이 큰 수는 JSON으로 내릴 때 파싱에서 오류가 날 수 있다고 문자열로 내리는 것을 봤는데 자바스크립트에서 부동소수점 수를 파싱할 때 부정확해지기 때문이라는 것을 알게됐다.
- 상위 호환성과 하위 호환성은 API를 개발할 때도 중요한 개념이다. 클라이언트와 호환성이 맞지 않아서 오류가 난 경우가 몇번 있어서 항상 버저닝을 하거나 호환성을 유지하는 방식으로 API를 개발했다.
- 스키마의 호환성이 중요하다는 것을 느꼈다.